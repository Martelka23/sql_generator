{
  "name": "ts-sql-generator",
  "version": "0.8.0",
  "author": "martelka",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "description": "",
  "scripts": {
    "build": "rm -rf dist/ && npx tsup src --format cjs,esm --dts --clean",
    "compile": "rm -rf dist/ && npx tsc",
    "start": "node dist/index.js",
    "test": "rm -rf dist/ && npx tsc && node dist/index.js"
  },
  "keywords": [],
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}