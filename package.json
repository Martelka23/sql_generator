{
  "name": "ts-sql-generator",
  "version": "0.6.2",
  "author": "martelka",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "description": "",
  "scripts": {
    "build": "rm -rf dist/ && npx tsup src --format cjs,esm --dts --clean",
    "compile": "rm -rf dist/ && npx tsc"
  },
  "keywords": [],
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}
